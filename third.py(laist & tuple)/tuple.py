#the main difference between list and tuple is that list is mutable and tuple is immutable.
#if we have single element in tuple then we separate this by comma like this (1,) and if we can't do this then python consider this as an integer value



tuple=(3,4,2,1,5)
print(type(tuple))
print(tuple)